[pytest]
DJANGO_SETTINGS_MODULE = bikes_project.settings
# --ds overrides any stray DJANGO_SETTINGS_MODULE exported in the shell
addopts = --ds=bikes_project.settings
testpaths = tests
python_files = test_*.py
